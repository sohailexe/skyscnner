// src/types.ts
export interface Flight {
  airline: string;
  from: string;
  to: string;
  departureTime: string;
  arrivalTime: string;
  totalDuration: string;
  numberOfStops: number;
  cabinClass: string;
  totalPrice: string;
  currency: string;
}
